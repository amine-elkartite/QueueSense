import {Router} from 'express';import {dashboardController} from '../controllers/dashboard.controller.js';import {requireAuth} from '../middleware/auth.js';import {asyncHandler} from '../utils/asyncHandler.js';
export const dashboardRouter=Router();dashboardRouter.use(requireAuth);dashboardRouter.get('/stats',asyncHandler(dashboardController.stats));dashboardRouter.get('/live-queues',asyncHandler(dashboardController.live));dashboardRouter.get('/predictions',asyncHandler(dashboardController.predictions));dashboardRouter.get('/api-usage',asyncHandler(dashboardController.usage));
