import {Router} from 'express';import {developerController} from '../controllers/developer.controller.js';import {requireAuth} from '../middleware/auth.js';import {allowRoles} from '../middleware/roles.js';import {asyncHandler} from '../utils/asyncHandler.js';
export const developerRouter=Router();developerRouter.use(requireAuth,allowRoles('DEVELOPER','ADMIN'));developerRouter.get('/api-keys',asyncHandler(developerController.list));developerRouter.post('/api-keys',asyncHandler(developerController.create));developerRouter.delete('/api-keys/:id',asyncHandler(developerController.revoke));developerRouter.post('/api-keys/:id/regenerate',asyncHandler(developerController.regenerate));
