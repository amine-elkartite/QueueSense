import type { Request,Response } from 'express'; import { authService } from '../services/auth.service.js'; import { ok } from '../utils/response.js'; import { prisma } from '../config/prisma.js';
export const authController={ register:async(req:Request,res:Response)=>ok(res,await authService.register(req.body),{},201), login:async(req:Request,res:Response)=>ok(res,await authService.login(req.body.email,req.body.password)), refresh:async(req:Request,res:Response)=>ok(res,{tokens:await authService.refresh(req.body.refreshToken)}), logout:async(req:Request,res:Response)=>{await authService.logout(req.body.refreshToken);return ok(res,{loggedOut:true})}, me:async(req:Request,res:Response)=>ok(res,await prisma.user.findUnique({where:{id:req.auth!.userId},select:{id:true,firstName:true,lastName:true,email:true,role:true,status:true,createdAt:true}})) };
