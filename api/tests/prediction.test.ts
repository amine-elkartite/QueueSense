import{describe,it,expect}from'vitest';import{StatisticalWaitTimePredictor,crowdLevel}from'../src/prediction/StatisticalWaitTimePredictor';describe('prediction algorithm',()=>{it('returns bounded confidence and non-negative wait',async()=>{const p=new StatisticalWaitTimePredictor();const now=new Date('2026-09-06T10:00:00Z');const history=Array.from({length:80},(_,i)=>({recordedAt:new Date(now.getTime()-i*3600000),estimatedWaitTime:10+i%8,peopleWaiting:5+i%6}));const r=await p.predict({now,target:new Date(now.getTime()+3600000),peopleWaiting:12,peopleBeingServed:3,averageServiceTime:4,capacity:30,history,incidentActive:false,openingHours:[]});expect(r.waitTime).toBeGreaterThanOrEqual(0);expect(r.confidence).toBeGreaterThanOrEqual(0);expect(r.confidence).toBeLessThanOrEqual(1)});it('classifies crowd level',()=>expect(crowdLevel(30,30,35,15)).toBe('VERY_HIGH'))});
