import{useQuery}from'@tanstack/react-query';import api from'../services/api';export function AdminResourcePage({title,endpoint,description}:{title:string;endpoint:string;description:string}){const{data=[],isLoading}=useQuery({queryKey:['admin',endpoint],queryFn:async()=>(await api.get(endpoint)).data.data});return <><div className="page-head"><div><h1>{title}</h1><p>{description}</p></div></div><div className="card">{isLoading?'Loading…':<><strong>{data.length} records</strong><div className="resource-list">{data.slice(0,50).map((row:any,i:number)=><div className="resource-row" key={row.id??i}><span>{row.name??row.email??row.type??row.id}</span><small>{row.status??row.role??row.owner?.email??''}</small></div>)}</div></>}</div></>}
