import type{LucideIcon}from'lucide-react';export function StatCard({label,value,delta,icon:Icon}:{label:string;value:string|number;delta?:string;icon:LucideIcon}){return <div className="card stat-card"><div><span className="muted">{label}</span><strong>{value}</strong>{delta&&<small className="positive">{delta}</small>}</div><div className="icon-box"><Icon size={22}/></div></div>}
