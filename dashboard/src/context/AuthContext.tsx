import {createContext,useContext,useEffect,useMemo,useState,type ReactNode} from 'react';import api from '../services/api';import type{User}from'../types';type C={user:User|null;loading:boolean;login:(e:string,p:string)=>Promise<void>;logout:()=>Promise<void>};const AuthContext=createContext<C|null>(null);export function AuthProvider({children}:{children:ReactNode}){const[user,setUser]=useState<User|null>(null);const[loading,setLoading]=useState(true);const load=async()=>{try{if(localStorage.getItem('qs_access')){const r=await api.get('/auth/me');setUser(r.data.data)}}finally{setLoading(false)}};useEffect(()=>{void load()},[]);const login=async(email:string,password:string)=>{const r=await api.post('/auth/login',{email,password});localStorage.setItem('qs_access',r.data.data.tokens.accessToken);localStorage.setItem('qs_refresh',r.data.data.tokens.refreshToken);setUser(r.data.data.user)};const logout=async()=>{const refreshToken=localStorage.getItem('qs_refresh');try{if(refreshToken)await api.post('/auth/logout',{refreshToken})}finally{localStorage.removeItem('qs_access');localStorage.removeItem('qs_refresh');setUser(null)}};const value=useMemo(()=>({user,loading,login,logout}),[user,loading]);return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>}export const useAuth=()=>{const c=useContext(AuthContext);if(!c)throw new Error('AuthProvider missing');return c};
